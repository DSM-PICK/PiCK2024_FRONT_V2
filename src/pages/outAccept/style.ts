import { styled } from "styled-components";
import { theme } from "styles/theme";

export const OutAcceptContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  row-gap: 40px;
  column-gap: 60px;
`;
